<h1><%= @pet.name %></h1>
  <p><%= @pet.description %></p>
  <p><%= @pet.approximate_age %></p>
  <p><%= @pet.sex %></p>
  <p><%= @pet.status %></p>
  <img src= <%= @pet.image %>>
<%= link_to 'Update', "/pets/#{@pet.id}/edit" %>
<%= button_to 'Delete', "/pets/#{@pet.id}", method: :delete %>
